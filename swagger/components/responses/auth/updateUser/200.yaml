description: Successfully update
content:
  application/json:
    schema:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          $ref: ../../../schemas/user.yaml
