description: Fail update user info
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: 'Bad request'
        data:
          type: object
          properties:
            message:
                  type: string
                  example: 'At least one field must be filled'