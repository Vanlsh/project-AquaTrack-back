description: Fail register
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 500
        message:
          type: string
          enum:
            [
              'Email in use',
              'Please verify your email',
              'Email is required',
              '"password" is required',
            ]
