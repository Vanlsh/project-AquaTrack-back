description: Successfully register
content:
  application/json:
    schema:
      type: object
      required:
        - data
      properties:
        data:
          type: object
          properties:
            user:
              type: object
              properties:
                email:
                  type: string
                  example: 'user@mail.com'
