description: Fail count users
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: 'Bad request'
        data:
          type: object
          properties:
            message:
              type: string
              example: 'Fail get count'
