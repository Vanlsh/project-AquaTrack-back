description: Fail count users
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 500
        message:
          type: string
          enum:
            - Invalid email or password
            - Email is required
            - "password" is required
            - Email or password is wrong
