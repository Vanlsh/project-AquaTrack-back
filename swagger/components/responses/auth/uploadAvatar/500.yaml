description: Fail avatar
  application/json:
    schema:
      type: object
      properties:
        status:
          type: number
          example: 500
        message:
          type: string
          oneOf:
            - example: "Not authorized"
            - example: "File not provided"

